I am interested in building a viewer, configurator application which would be used to view models from different industries like granite monument, granite sculptures, automotive, industrial supplies, equipment provider similar to grainger. The Viewer should provide best viewing experience for the parts model. Viewer should provide seamless option for lighting, camera angles, orbit, pan zoom. It should provide a interactive environment to explore the model. The viewer should be available as standalone application and also reusable viewer component. The viewer should preload the model for better performance. The viewer should provide better performance and should not show lag when performing operation in interactive environment. The viewer should be able to fetch models dynamically from the server/cloud application. The application should allow the user to transition to configurator. The configurator should allow the user to modify and perform additional configuration changes to the model. Based on the user changes to the model, new model should be generated in the backend and dynamically displayed in the UI. Based on the different configuration options, if the configuration would take more time by the cad application, then UI should be interactive and allow the user to perform parallel actions. The model should by generated by the configurator backend while UI should be notified. UI should be able to pick the new model and display in the UI. The notification should be seamless for the user. User should be able to continue to perform existing action or transition to the configurator viewer where he left off earlier. The UI should be able to maintain the state. Provide me a detailed plan for the project with choices of the UI framework, backend languages, database, cloud options. Based on the option selection we can proceed later in creating the project plan